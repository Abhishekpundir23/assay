# Assay — landing page

One-page marketing site for Assay (AI reliability audits). Static HTML, no build step.

- Edit: `index.html` (single file, inline CSS).
- **Before sharing:** replace `you@example.com` with your real contact email (find-and-replace; it appears in the CTA links and footer).
- Hosted on GitHub Pages from the `main` branch.

To preview locally, just open `index.html` in a browser.
